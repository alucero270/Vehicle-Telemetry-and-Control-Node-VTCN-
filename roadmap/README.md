# Roadmap

This directory holds future-facing planning material that should not dominate the
repository root before implementation exists.

## Layout

- `components/` preserves placeholders for planned subsystems

Active implementation remains at the repository root in:
- `vtcn-daemon/`
- `docs/`
- `toolchain/`
