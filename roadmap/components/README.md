# Planned Components

These directories represent planned subsystems that are not yet active enough to justify
top-level placement.

Move a component back to the repository root only when it contains implemented code,
validated tooling, or documentation that materially changes the current project truth.
